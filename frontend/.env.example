# Backend API URL for server-side requests
# docker-compose: http://backend:8080 / local: http://localhost:8080 (next.config.ts default)
# For local dev, create .env.local with BACKEND_URL=http://localhost:8080
BACKEND_URL=http://backend:8080
