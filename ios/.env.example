# API base URL
# Debug builds default to http://localhost:8080 via #if DEBUG in APIClient.swift
# Release builds default to https://api.muff.app
# To override, change the defaultBaseURL in APIClient.swift or use xcconfig
API_BASE_URL=http://localhost:8080
